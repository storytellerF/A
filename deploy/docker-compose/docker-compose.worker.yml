services:
  worker:
    build:
      context: ../../
      dockerfile: worker.Dockerfile
      args:
        BUILD_ON: ${BUILD_ON}
#    restart: unless-stopped
    env_file:
      - ../../deploy/${FLAVOR}.env
