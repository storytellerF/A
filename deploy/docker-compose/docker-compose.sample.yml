services:
  app-wasm:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - ${APP_WASM_PORT:-8080}:80
    volumes:
      - ../../app/composeApp/build/dist/wasmJs/productionExecutable:/usr/share/nginx/html:ro
      - ./app-wasm.nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      server:
        condition: service_started
      ws:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost/"]
      interval: 5s
      timeout: 3s
      retries: 12
