services:
  main:
    build:
      context: ../
      dockerfile: ./dev.Dockerfile
    ports:
      - "6081:6080" # noVNC web interface
      - "5902:5901" # VNC direct connection
      - "5556:5555" # ADB
      - "4724:4723" # Appium
    environment:
      - VNC_PASSWD=password
      - TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal
    volumes:
      - ..:/workspace/A:cached
      - ../../AData:/workspace/AData:cached
      - /var/run/docker.sock:/var/run/docker.sock  # 宿主机 Docker 套接字
      - sdk_data:/opt/android/sdk
      - a-bashhistory:/commandhistory
      - avd_data:/home/ubuntu/.android/avd
      - gradle_data:/home/ubuntu/.gradle
      - konan_data:/home/ubuntu/.konan
      - m2_data:/home/ubuntu/.m2
    shm_size: '2gb' # Allocate more shared memory
    privileged: true # Enable KVM acceleration
    # devices:
    #   - /dev/kvm

volumes:
  avd_data:
  a-bashhistory:
  sdk_data:
    name: sdk_data
    external: true
  gradle_data:
  konan_data:
  m2_data: